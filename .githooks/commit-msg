#!/bin/sh

# Commit message hook to validate commit messages with commitlint
# Based on the original Husky commit-msg hook

docker compose run --rm -T --entrypoint npx app --no -- commitlint --edit "$1"
