{
  "mcpServers": {
    "laravel-boost": {
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "mw-app",
        "php",
        "/var/www/artisan",
        "boost:mcp"
      ]
    }
  }
}